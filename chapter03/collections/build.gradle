jar {
    manifest {
        attributes(
                "Created-By": "Isaac Bao",
                "Specification-Title": "spring-dk",
                "Main-Class" : "com.isaac.ch3.annotated.CollectionInjection",
                "Class-Path": configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
