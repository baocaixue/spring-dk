jar {
    manifest {
        attributes(
                "Created-By": "Isaac Bao",
                "Specification-Title": "spring-dk",
                "Main-Class" : "com.isaac.ch3.HierarchicalAppContextUsage",
                "Class-Path": configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
