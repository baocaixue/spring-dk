dependencies {
    compile db.h2, db.dbcp2
}
jar {
    manifest {
        attributes("Created-By"      : "Isaac Bao",
                "Specification-Title": "spring-dk",
                "Main-Class"         : "com.isaac.ch18.SingerJobDemo",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}