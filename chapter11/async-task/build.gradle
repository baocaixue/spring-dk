dependencies {
    compile spring.context
}

jar {
    manifest {
        attributes("Created-By"      : "Isaac Bao",
                "Specification-Title": "spring-dk",
                "Main-Class"         : "com.isaac.ch11.AsyncTaskDemo",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}